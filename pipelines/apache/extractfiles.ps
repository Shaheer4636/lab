set -e
ZIP_PATH="$(System.DefaultWorkingDirectory)/bsro-apache-configs/configs.zip"
OUT_DIR="$(System.DefaultWorkingDirectory)/extracted"

rm -rf "$OUT_DIR"
mkdir -p "$OUT_DIR"

unzip -o "$ZIP_PATH" -d "$OUT_DIR"
echo "Extracted to $OUT_DIR"
